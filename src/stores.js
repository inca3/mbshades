import { writable } from "svelte/store";

export const moneyboys = writable([]);
export const filteredMoneyboys = writable([]);
export const filteredBoy = writable({});
export const userLogin = writable(true);
